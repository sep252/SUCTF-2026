采题注意事项：

1.docker版本20.10

2.flag不要写到环境变量里。

3.资源放到本地，尤其前端资源放到本地。

4.请勿使用赛题服务进程为主进程，请使用类似start.sh来启动赛题服务进程，并使用’tail -f /dev/null’类似命令将start.sh进程卡住，尽量保证使用docker run -d能直接启动容器或杀掉赛题服务进程后容器存活。

5.目前平台只支持最基本的docker run -itd 启动，不能有其他附加的启动命令。

