services:
  cloudhook-service:
    build: .
    image: su_uri:latest
    container_name: cloudhook-service
    restart: unless-stopped
    ports:
      - "8080:8080"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - /:/host
